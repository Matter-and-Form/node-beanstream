/*
	Template for a credentials.js file for use in `test.js`

	1) Duplicate this file
	2) Rename to `credentials.js`
	3) Fill in your credentials.

	Note: credentials.js is in .gitignore, thus untracked;

	Currently supported: `username` & `password`, or `merchant_id` & `passcode`;
*/

module.exports = {
	//username: "USERNAME",
	//password: "PASSWORD",
	merchant_id: "0000000000000000",
	passcode: "XXXXXXXXXXXXXXX"
};